using System;
using NLog;
namespace Demo.Service.Base
{
	public class BaseService: IBaseService
	{
		//protected Logger _log { get; private set; }

		//public BaseService(Type declaringType)
		//{
		//	_log = LogManager.GetLogger(declaringType.FullName);
		//}

        public BaseService()
        {
            
        }
    }
}

